#version 460

layout (local_size_x = 1, local_size_y = 1) in;

layout (std430, binding = 0) buffer color_buf 
{
    vec4 c[];
};


void main() {
    c[(gl_GlobalInvocationID.x * 256) + gl_GlobalInvocationID.y] = vec4(1.0, 0.0, 0.0, 1.0);
}
